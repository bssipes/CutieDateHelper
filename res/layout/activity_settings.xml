<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".SettingsActivity" >

    <Spinner
        android:id="@+id/DefaultDivision_S"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/default_divisions_array"
        android:prompt="@string/division_prompt" />

    <Spinner
        android:id="@+id/SearchDivision_S"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/search_divisions_array"
        android:prompt="@string/search_prompt" />

    <CheckBox
        android:id="@+id/gen2jul_cb"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Display Gen2 Products as Julian Days?" />

    <CheckBox
        android:id="@+id/gen3jul_cb"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Display Gen3 Products as Julian Days?" />

    <Button
        android:id="@+id/SaveAndSubmit_Button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Defaults and Submit" />

</LinearLayout>